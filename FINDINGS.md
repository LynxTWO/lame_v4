# LAME v4 — Findings, Methods, and Results

A living record of the quality-first effort to modernize the LAME MP3 encoder toward a
version 4 release. Written for two readers at once: a **layman** who wants to understand what
we're doing and trust the results, and an **engineer** who needs to verify and extend them.
Layman-friendly summaries lead each section; the exact commands, numbers, and code references
follow.

> **One-line summary:** MP3 is a fixed, 25-year-old format, but *how well an encoder uses it*
> still has headroom. We built an honest way to *measure* audio quality, then used it to find
> and fix a real quality bug in LAME 3.100 — verified on synthetic torture signals, real
> music, and the EBU's professional test set.

---

## 1. What this is, in plain language

**The goal (from the project owner):** improve the *quality* of LAME's MP3 encoding "at all
reasonable costs." Speed is welcome, but never at the expense of quality — modern CPUs have
16–32 cores sitting idle during an encode, so we can afford to make the encoder *think harder*
about every decision.

**Why quality can still improve on a frozen format:** encoding an MP3 is a giant optimization
problem — given a bit budget, decide how to round off ("quantize") the audio so that the
error you introduce hides *underneath* sounds you can already hear (this is "masking"). LAME's
choices here were tuned on 2002-era hardware with shortcuts for speed. With more compute we
can search harder for choices that put the error where you're least likely to notice it.

**The golden rule we hold ourselves to:** we don't get to *say* something sounds better — we
have to *measure* it, on hard material, reproducibly, before we believe it. Everything below
is built around that.

---

## 2. How we measure quality (the foundation)

Everything rests on being able to answer "did quality go up or down?" objectively. We built
three independent tools, all in `tests/`. **We built and validated all of this before changing
a single line of encoder code.**

### 2a. The bit-exact gate — "did we change something we didn't mean to?"

**Layman:** a tripwire. Some changes (making the encoder faster, tidying code) must produce the
*exact same MP3* as before. This tool encodes a fixed set of test sounds and takes a
fingerprint (a hash) of every output. If a "safe" change alters even one byte, the tripwire
trips.

**Engineer:** `tests/regress.ps1` encodes `tests/regress_corpus` (a deterministic synthetic set
regenerated by `tests/corpusgen`) at V0/V2/V5, CBR 320/128, `-q0` CBR320, ABR192, and stores
SHA-256 of both the MP3 bytes and the decoded PCM. 70-case baseline in `tests/baseline.json`.
`pwsh tests/regress.ps1` compares; nonzero exit on any drift.

### 2b. The perceptual meter — "how much of the error can you actually hear?"

**Layman:** the important tool. It takes the original audio and the MP3'd-then-decoded audio,
and measures how much of the encoding error pokes *above* the threshold of what's masked —
i.e. how audible the damage is. Lower is better (more transparent). Crucially, it uses its
*own* model of human hearing, **not** LAME's — so if we make LAME cleverer, it can't just
"mark its own homework."

**Engineer:** `tests/nmr` (a small .NET tool) computes a noise-to-mask ratio in the decoded
domain: per critical band (Bark scale), it derives a masking threshold from the *original*
(band energy + a spreading function + absolute threshold in quiet), measures the
original-vs-decoded error energy, and reports `meanNMRdb` (mean over all band-frames; lower =
more transparent), plus audible-fraction, audible-mean, and max. It cross-correlates to
compensate for MP3 encoder/decoder delay before comparing. It is deliberately independent of
LAME's psychoacoustic model, so an encoder change can't game it.

**Why you can trust it (validation):**
- **Monotone with bitrate** — on a music sample it scores 128 kbps at −3.5 dB and 320 kbps at
  −15.3 dB (more bits → provably less audible noise). A broken metric wouldn't do this.
- **Self-transparent** — comparing any file to *itself* scores ≈ −100 dB with zero audible
  bands. Identical in, "perfect" out, as it must be.

### 2c. The A/B harness — "which of two encoders/settings is better?"

**Layman:** the scoreboard. Give it two encoders (or two settings), and it encodes every test
sound with both, scores each with the perceptual meter, and prints who won by how much on each
sound and overall.

**Engineer:** `tests/abtest.ps1 -A base\lame.exe -B variant\lame.exe -Setting '-q 0 -b 128'`
(optionally `-SettingB` to compare two *settings*). Prints per-file and corpus-mean
`meanNMRdb` delta; **delta < 0 means B is more transparent at equal bitrate.** A=B self-check
reads 0.000 across the corpus (determinism confirmed).

### 2d. The test material (corpora)

**Layman:** we test on three kinds of material so we're not fooling ourselves: (1) synthetic
"torture" signals we generate, (2) real music, and (3) the professional standard set the audio
industry itself uses.

**Engineer:**
- `tests/regress_corpus/` — 10 deterministic synthetic critical signals (impulse trains,
  transients, sparse HF tonals, sweeps, pink/white noise, plucked harmonics, near-Nyquist
  square, silence+clicks, busy mix). Used by the bit-exact gate.
- `tests/corpus/` — the synthetic set plus **real user-supplied tracks** (incl. *Tom's Diner*
  a cappella, a legendary MP3-development killer sample).
- `tests/corpus/SQAM/` — the **EBU SQAM** set (Sound Quality Assessment Material): 70
  professional critical recordings (solo instruments, voices, ensembles), 44.1 kHz/16-bit
  stereo. The reference material real codec developers evaluate against.
- All corpus WAVs are git-ignored (regenerable and/or copyrighted); the generators and the
  committed baseline hashes are what make results reproducible.

---

## 3. Findings

### Finding 1 (major): LAME 3.100's `-q0` is *worse* than `-q4` at CBR/ABR — confirmed and fixed

**Layman:** LAME's quality dial goes from `-q9` (fast, rough) to `-q0` (slow, best). You'd
expect `-q0` to always sound best. It doesn't: at fixed-bitrate modes (CBR/ABR), `-q0` is
*worse* than the mid setting `-q4` on real music. We confirmed this on the owner's own tracks
and on the professional SQAM set, found *why*, and fixed it — and the fix makes `-q0` the best
setting again, as it should be.

**How we found it:** an external tip claimed `-q0..-q3` can regress vs `-q4` at CBR/ABR. Rather
than take it on faith, we tested it (`abtest.ps1` with `-SettingB`): stock `-q4` vs stock
`-q0` at CBR 128. Result — `-q0` **worse by +0.333 dB** on average, and much worse on real
music (400 Lux +0.44, Tom's Diner +0.40, sample20 +1.37 dB). Claim confirmed and quantified.

**Root cause (engineer):** at `-q0`, `libmp3lame/lame.c` sets a VBR-tuned noise-shaping config:
`noise_shaping_amp = 2` (amplify only the *single* worst scalefactor band per `outer_loop`
iteration) and `substep_shaping = 2` (pseudohalf half-stepping during amplification). Both
assume `outer_loop` iterates freely to convergence, which is true in VBR. Under a **fixed
CBR/ABR bit budget the loop terminates early** (out of bits), so this fine-grained shaping
never finishes distributing — leaving a worse noise spread than `-q4`'s broad
"amplify-all-bands" (`amp=0`) approach.

**The fix (engineer):** in the `-q0` branch of `init_qval` (`lame.c`), for bit-constrained modes
only (`cfg->vbr == vbr_off || vbr_abr`), route to the previously-**unused** `noise_shaping_amp
= 3` path — a coarse pass (`amp=1`) followed by a fine refine pass (`amp=2`), with
`full_outer_loop = 0` — and leave `substep_shaping` off. True VBR keeps its existing behavior.
Branch: `qa/fix-noiseshaping-cbr`, commit `0e346c9`.

**Results (fixed-`q0` vs stock-`q0`, `meanNMRdb`, negative = better):**

| Material | Setting | Files better | Corpus mean |
| --- | --- | --- | --- |
| Music + synthetic (32 files) | CBR 128 | 22 / 32 | ~−0.35 |
| Music + synthetic | CBR 320 | — | **−0.78** |
| Music + synthetic | ABR 192 | — | **−0.49** |
| **EBU SQAM (70 files)** | CBR 128 | **59 / 70** | **−0.926** |
| **EBU SQAM** | CBR 320 | **59 / 70** | **−1.576** |
| **EBU SQAM** | ABR 192 | **58 / 70** | **−0.608** |
| VBR (V0, V2) | — | — | **0.000 (untouched)** |

On SQAM the worst per-file regression is +0.10 dB (inaudible); the biggest single gain is
−15.4 dB (a sample where stock `-q0` badly misallocated). Real music improves −0.3 to −1.4 dB.

**Why it's safe (scoping proof):** the bit-exact gate shows byte-drift in **exactly** the 10
`-q0`-CBR test cases and *nothing else* — every VBR setting and every default-quality CBR/ABR
output is byte-identical to pristine 3.100. The change is surgically confined to the path it
targets, and it only improves it.

**Status:** validated three independent ways; awaiting the owner's decision to ship it to
default `-q0` and/or fold it into a `--quality max` mode, ideally after a quick human ABX on
one track (binaries ready: `output/lame_base.exe` = stock, `output/lame_fix.exe` = fixed).

### Finding 0 (minor): re-enabling the in-loop Huffman search (`best_huffman = 2`)

**Layman:** `-q0` turns off one of its own thorough sub-searches purely to save time (its own
source code says so). Turning it back on gives a small, real quality gain.

**Engineer:** stock `-q0` uses `use_best_huffman = 1`; the "inside loop, slow" mode (`= 2`,
`takehiro.c:747`) costs every candidate quantization with optimal Huffman partitioning, so the
search makes better bit-budget decisions. Experiment (branch `qa/deep-search`): +best_huffman=2
at `-q0 -b128` gave corpus-mean **−0.030 dB** (6/10 synthetic files better). Small but real,
and it exposed that `-q0` config leaks into `-V0/-V5` — which is why the production high-effort
mode must be a **dedicated flag**, not a change to existing settings.

---

## 4. How to reproduce every result

```
build.cmd                                   # build output\lame.exe (VS2022, 64-bit)
dotnet run --project tests\corpusgen -- tests\regress_corpus   # synthetic gate corpus
pwsh tests\regress.ps1 -UpdateBaseline      # lock bit-exact baseline from THIS build
pwsh tests\regress.ps1                      # verify a later build is bit-exact (exit 1 on drift)

dotnet build tests\nmr -c Release           # the perceptual meter
# A/B two builds or two settings on any corpus folder:
pwsh tests\abtest.ps1 -A output\lame_base.exe -B output\lame_fix.exe `
     -Setting '-q 0 -b 128' -Corpus tests\corpus\SQAM
```

To reproduce Finding 1 exactly: check out `qa/fix-noiseshaping-cbr`, `build.cmd`, copy the
result to `output/lame_fix.exe`, `git checkout master && build.cmd`, copy to
`output/lame_base.exe`, then run the `abtest.ps1` line above (and with `-Setting '-q 4 -b 128'
-SettingB '-q 0 -b 128'` to see the original regression).

---

## 5. Decisions and rationale (so a reviewer can challenge them)

- **Encoder-independent metric, not LAME's own.** Instrumenting LAME's internal noise objective
  would be *circular* (a search always improves the metric it optimizes) and risky (surgery in
  the fragile quantizer). An independent decoded-domain meter answers the real question and
  can't be gamed.
- **PEAQ/ViSQOL and human ABX are cross-checks, not the daily driver.** Trustworthy PEAQ builds
  are Linux-leaning; we keep them off the critical path so tooling risk can't block progress.
  The in-house meter is validated (§2b) and sufficient for *relative* A/B at equal bitrate.
- **Every behavior change is scoped and proven.** Bug fixes are gated to the exact mode they
  target and verified byte-isolated; nothing ships default-on without measurement and, for
  audible changes, human ABX.

---

## 6. Status and roadmap

- [x] Measurement foundation (bit-exact gate, perceptual meter, A/B harness) — built + validated.
- [x] Three-way test corpora incl. real music and EBU SQAM.
- [x] **Finding 1: CBR/ABR `-q0` regression — confirmed and fixed**, validated on SQAM.
- [ ] Owner decision: ship the fix to default `-q0` vs gate under `--quality max` (ABX first).
- [ ] `--quality max` mode: dedicated flag folding in the fix + best_huffman=2 + a deeper
      noise-allocation search (the big flagship lever).
- [ ] Podcast constrained-optimizer modes (ABR-centered, parallel legal-candidate search;
      192 kbps stereo / 96 kbps mono; never cap frames with `-B`).
- [ ] Multithreading (bit-exact) to *fund* the expensive search; AVX2 SIMD on the scalar hot
      loops; modern CMake/CI; fuzz the decoder.

---

## 7. Glossary (for the layman)

- **Quantization** — rounding the audio numbers to fit the bit budget; the rounding *is* the
  lossy part of MP3.
- **Masking / masking threshold** — sounds hide quieter sounds nearby in pitch/time; error kept
  under this threshold is inaudible.
- **Noise-to-mask (NMR)** — how far the encoding error pokes above the masking threshold. Below
  0 dB ≈ inaudible; our `meanNMRdb` is an average, lower = cleaner.
- **CBR / ABR / VBR** — Constant / Average / Variable BitRate. CBR and ABR aim at a fixed size;
  VBR aims at a quality level and lets size float.
- **`-q0` vs `-V0`** — `-q` is the encoder's *effort* (how hard it searches); `-V` is a VBR
  *quality* target. Different dials that interact.
- **Bit reservoir** — MP3's trick of "saving" unused bits from easy moments to spend on hard
  ones; capping frame size (`-B`) can rob it, which is why we don't do that.
